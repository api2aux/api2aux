# MCP Worker URL (required for the Hosted tab in MCP Tools dialog)
# Set this to your deployed api2aux-platform MCP worker URL to enable hosted MCP deployment.
# The MCP worker is not part of this repo — see api2aux-platform for self-hosting.
VITE_MCP_WORKER_URL=

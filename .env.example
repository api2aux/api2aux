# MCP worker URL for hosted deployments (see api2aux-platform; leave empty to disable)
VITE_MCP_WORKER_URL=

# Custom CORS proxy URL (leave empty to use the default /api-proxy/ same-origin path)
# VITE_CORS_PROXY_URL=https://my-proxy.example.com/

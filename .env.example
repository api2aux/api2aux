# MCP Worker URL — hosted MCP deployment (see api2aux-platform)
# Required for the Hosted tab in the MCP Tools dialog.
# The MCP worker is not part of this repo — see api2aux-platform for self-hosting.
VITE_MCP_WORKER_URL=

/**
 * buildUIPlan() — the main orchestrator.
 * Stub for Phase 1; full implementation in Phase 3.
 */
import type { UIPlan, BuildOptions } from './types'

export function buildUIPlan(
  _input: string | unknown,
  _options?: BuildOptions,
): UIPlan {
  throw new Error('buildUIPlan() not yet implemented — Phase 3')
}
